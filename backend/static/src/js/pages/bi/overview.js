import { initDashboardManager } from './dashboard-manager.js';

/**
 * @param {HTMLElement} page
 */
export function initOverview(page) {
  initDashboardManager(page);
}

export default { initOverview };
